<?php
// --- ЗАДАНИЕ 1: Типы данных и математика ---
echo "<h3>Задание 1</h3>";

$item = "Ноутбук";    // строка
$price = 45000;       // число
$discount = 0.15;     // дробь (скидка 15%)
$is_stock = true;     // булево

// Считаем цену со скидкой
$final_price = $price - ($price * $discount);

echo "Товар: $item <br>";
echo "Старая цена: " . $price . " руб.<br>";
echo "Цена со скидкой: " . $final_price . " руб.<br>";

// Математические функции
$num = -7.8;
echo "Модуль числа $num это " . abs($num) . "<br>";
echo "Округление в большую сторону: " . ceil($num) . "<br>";
echo "Случайное число: " . rand(1, 100) . "<br>";

// Дата
echo "Отчет сформирован: " . date("d.m.Y") . "<br>";


// --- ЗАДАНИЕ 2: Массивы и циклы ---
echo "<h3>Задание 2</h3>";

// Обычный массив (оценки)
$marks = [9, 8, 7, 9, 8, 2];
$marks[] = 4; // добавили еще одну в конец

echo "Все оценки: ";
foreach ($marks as $m) {
    if ($m <= 3) {
        echo "<b style='color:red'>$m</b> "; // выделяем плохие
    } else {
        echo "$m ";
    }
}

// Ассоциативный массив
$user = [
    "имя" => "Екатерина",
    "курс" => 3,
    "группа" => "ПЗТ-42"
];

echo "<br>Данные студента:<br>";
echo "Имя: " . $user["имя"] . ", Группа: " . $user["группа"] . "<br>";


// --- ЗАДАНИЕ 3: Функции и строки ---
echo "<h3>Задание 3</h3>";

// Функция для приветствия
function say_hello($name, $time) {
    $s = "Добрый день, " . $name . "! Сейчас " . $time;
    return $s;
}

$current_time = date("H:i");
echo say_hello($user["имя"], $current_time);

echo "<br><br>";

// Работа со строками
$text = "  Это учебная лабораторная работа по PHP  ";
$text = trim($text); // убрали пробелы по краям
echo "Длина строки: " . strlen($text) . " символов<br>";
echo "Верхний регистр: " . mb_strtoupper($text) . "<br>";

// Пример Heredoc
$doc = <<<LABEL
Тут можно писать длинный текст 
с кавычками "вот так" и переменными:
группа {$user['группа']}.
LABEL;

echo nl2br($doc); // nl2br чтобы сохранились переносы строк
?>